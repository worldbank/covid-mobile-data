# Download GADM

getData('GADM', country='MOZ', level=0, path = GADM_PATH)
getData('GADM', country='MOZ', level=1, path = GADM_PATH)
getData('GADM', country='MOZ', level=2, path = GADM_PATH)
getData('GADM', country='MOZ', level=3, path = GADM_PATH)

